# УЛН. Регистрация водителей v1.5

Система регистрации водителей для распределительного центра с поддержкой PWA.

## 🚀 Функции

- ✅ Многошаговая регистрация водителей
- 📱 PWA (Progressive Web App) с оффлайн-поддержкой
- 🔔 Push-уведомления
- 👤 Личный кабинет водителя
- 📊 История регистраций
- 🔄 Синхронизация данных
- 🎨 Адаптивный дизайн

## 📁 Структура проекта

```
reg_driver_ULN/
├── index.html          # Главная страница
├── app.js             # Основной JavaScript (старый)
├── config.js          # Конфигурация (новый модуль)
├── utils.js           # Вспомогательные функции (новый модуль)
├── api.js             # API функции (новый модуль)
├── styles.css         # Основные стили
├── inline-styles.css  # Встроенные стили (выделены отдельно)
├── manifest.json      # PWA манифест
├── service-worker.js  # Service Worker
└── icons/             # Иконки PWA
    ├── icon-16x16.png
    ├── icon-32x32.png
    └── ...
```

## 🔧 Установка и запуск

### Локальный запуск

```bash
# С помощью Python
python -m http.server 8000

# Или с помощью Node.js
npx http-server -p 8000

# Или с помощью PHP
php -S localhost:8000
```

Затем откройте в браузере: `http://localhost:8000/reg_driver_ULN/`

## 🏗️ Сборка для продакшена

### Минификация CSS и JS

Используйте следующие инструменты для минификации:

#### CSS минификация:
```bash
# С помощью clean-css
cleancss -o styles.min.css styles.css inline-styles.css

# Или с помощью csso
csso styles.css inline-styles.css -o styles.min.css
```

#### JS минификация:
```bash
# С помощью terser
terser config.js utils.js api.js app.js -o app.min.js

# Или с помощью uglify-js
uglifyjs config.js utils.js api.js app.js -o app.min.js
```

### Автоматизированная сборка

Создайте `package.json`:
```json
{
  "scripts": {
    "build": "mkdir -p dist && cp -r * dist/ && cleancss -o dist/styles.min.css styles.css inline-styles.css && terser config.js utils.js api.js app.js -o dist/app.min.js",
    "dev": "python -m http.server 8000"
  }
}
```

## 📱 Тестирование PWA

### Lighthouse аудит

1. Откройте Chrome DevTools
2. Перейдите во вкладку "Lighthouse"
3. Выберите категории: Performance, PWA, Best Practices
4. Нажмите "Generate report"

### Ручное тестирование PWA

1. **Установка на устройство:**
   - Откройте в Chrome/Safari
   - Должна появиться подсказка "Добавить на главный экран"
   - Установите приложение

2. **Оффлайн-тестирование:**
   - Откройте установленное PWA
   - Перейдите в оффлайн-режим в DevTools
   - Проверьте работу основных функций

3. **Push-уведомления:**
   - Разрешите уведомления при первом запуске
   - Проверьте получение уведомлений

### Тестирование на мобильных устройствах

1. **Android Chrome:**
   - Откройте сайт
   - Нажмите меню (три точки) → "Добавить на главный экран"
   - Запустите установленное PWA

2. **iOS Safari:**
   - Откройте сайт
   - Нажмите кнопку "Поделиться" → "Добавить на главный экран"
   - Запустите установленное PWA

## 🔒 Безопасность

- ✅ HTTPS обязательна для PWA
- ✅ Валидация вводимых данных
- ✅ Content Security Policy рекомендуется
- ✅ XSS защита через экранирование

## 🚀 Развертывание

### На веб-сервер

1. Загрузите все файлы на сервер
2. Настройте HTTPS
3. Проверьте работу Service Worker
4. Протестируйте PWA функции

### На GitHub Pages

```bash
# Создайте репозиторий
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/username/repo.git
git push -u origin main
```

Включите GitHub Pages в настройках репозитория.

## 📊 Мониторинг

### Ключевые метрики PWA

- **Installability:** Доля пользователей, установивших PWA
- **Usage:** Время использования PWA vs веб-версии
- **Engagement:** Push-уведомления, повторные визиты

### Service Worker

Проверьте в DevTools → Application → Service Workers:
- ✅ Зарегистрирован
- ✅ Активен
- ✅ Обновляется автоматически

## 🐛 Устранение неполадок

### PWA не устанавливается

1. Проверьте HTTPS
2. Проверьте manifest.json
3. Проверьте наличие иконок
4. Проверьте Service Worker

### Push-уведомления не работают

1. Проверьте разрешение браузера
2. Проверьте HTTPS
3. Проверьте код уведомлений

### Оффлайн не работает

1. Проверьте Service Worker
2. Проверьте кэширование в DevTools
3. Проверьте оффлайн-страницы

## 📝 API документация

### Основные эндпоинты

- `register_driver` - Регистрация водителя
- `get_driver_history` - Получение истории
- `get_pwa_notifications` - Получение уведомлений
- `get_top_data` - Получение справочных данных

## 🆕 Изменения в версии 1.5

- ✅ Исправлены ссылки в manifest.json
- ✅ Синхронизированы версии во всех файлах
- ✅ Разделен большой app.js на модули
- ✅ Выделены встроенные стили в отдельный файл
- ✅ Улучшена архитектура кода

## 📞 Поддержка

При возникновении проблем создайте issue в репозитории или свяжитесь с разработчиком.
